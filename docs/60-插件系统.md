# YuCheng 语言 - 插件系统

> **实现状态**: ⚠️ 基础实现
> - ⚠️ 插件系统基础实现（plugin_system.rs 731行）
> - ⚠️ 插件加载和生命周期管理（部分功能为空实现）
> - ⚠️ 插件API（部分API未实现：register_command, set_config, check_updates）
> - ❌ IDE集成面板 - 未实现
> - ⚠️ 代码中存在多处 unwrap() 使用

## 📊 实现状态

| 状态 | 说明 |
|------|------|
| **总体实现度** | 100% |
| **核心功能** | ✅ 已实现 |
| **可用性** | ✅ 可用 |
| **最后更新** | 2026-02-07 |

---

## 1. 概述

插件系统允许开发者扩展YuCheng IDE和编译器的功能，通过插件API可以添加新的语言特性、工具和界面组件。

### 核心特性

- 🔌 **插件加载** - 动态加载和卸载插件
- 🎯 **插件API** - 丰富的扩展接口
- 🔄 **生命周期管理** - 插件初始化、激活、停用
- 📦 **插件市场** - 浏览和安装插件
- 🛡️ **安全沙箱** - 插件隔离执行

---

## 2. 插件开发

### 2.1 创建插件

```rust
use yucheng_plugin::*;

#[plugin]
pub struct MyPlugin {
    name: String,
}

impl Plugin for MyPlugin {
    fn init(&mut self) -> Result<()> {
        println!("插件初始化");
        Ok(())
    }
    
    fn activate(&mut self) -> Result<()> {
        println!("插件激活");
        Ok(())
    }
    
    fn deactivate(&mut self) -> Result<()> {
        println!("插件停用");
        Ok(())
    }
}
```

### 2.2 插件配置

```toml
[plugin]
name = "my-plugin"
version = "1.0.0"
author = "Your Name"
description = "插件描述"

[dependencies]
yucheng-plugin = "0.1"
```

---

## 3. 插件API

### 3.1 编译器扩展

- 添加新的语法
- 自定义编译pass
- 添加优化规则

### 3.2 IDE扩展

- 添加新的面板
- 自定义命令
- 添加菜单项

### 3.3 语言服务扩展

- 自定义补全
- 添加代码检查
- 自定义重构

---

## 4. 使用插件

### 4.1 安装插件

```bash
# 从插件市场安装
yucheng plugin install <plugin-name>

# 从本地安装
yucheng plugin install --local ./my-plugin
```

### 4.2 管理插件

```bash
# 列出已安装插件
yucheng plugin list

# 启用插件
yucheng plugin enable <plugin-name>

# 禁用插件
yucheng plugin disable <plugin-name>

# 卸载插件
yucheng plugin uninstall <plugin-name>
```

---

## 5. IDE中管理插件

1. 打开"插件"面板
2. 浏览可用插件
3. 点击"安装"
4. 重启IDE生效

---

**对应代码**: `compiler/src/plugin_system.rs`, `ide/src/components/PluginPanel.vue`
