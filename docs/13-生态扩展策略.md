# YuCheng 语言 - 生态扩展策略

> **实现状态**: 📋 规划文档
> - 📋 这是生态扩展的战略规划
> - ❌ Web前端编译未实现
> - ❌ 数据科学桥接未实现
> - ❌ 大型GUI支持未实现
> - 📋 用于指导未来生态建设

## 1. 核心原则

YuCheng 扩展到 Web 前端、数据科学、大型 GUI 等领域的核心策略是：**桥接而非重造**。

| 原则 | 说明 |
|------|------|
| **核心不变** | 类型系统、内存模型、契约机制绝不为"方便"妥协 |
| **桥接生态** | 通过 C ABI / FFI / 编译目标，接入现有成熟生态 |
| **DSL 专用化** | 在特定领域提供专用语法，但底层仍是同一套 IR |
| **工具链赋能** | 编译器/IDE 自动完成繁琐转换，开发者只写高层抽象 |

> 不是让语言本身变成 React 或 Python，而是让它能"优雅地驱动"这些生态。

---

## 2. Web 前端开发

### 2.1 策略：编译到前端框架

**不要做：**
- ❌ 自研虚拟 DOM
- ❌ 重写 CSS 引擎
- ❌ 模仿 JSX

**正确做法：将 `.ycv` 编译为成熟前端框架的代码**

### 2.2 方案 A：编译到 Vue（推荐）

`.ycv` 文件结构映射为 `.vue` 单文件组件：

```御程
// counter.ycv

视图 计数器 {
    状态 count: 整数 = 0
    
    副作用 func 增加() {
        count += 1
    }
    
    渲染 {
        按钮 {
            文字: "点击 " + 转文本(count) + " 次"
            点击 => 增加()
        }
    }
}
```

↓ 编译为 ↓

```vue
<template>
  <button @click="增加">点击 {{ count }} 次</button>
</template>

<script setup>
import { ref } from 'vue'

const count = ref(0)

function 增加() {
  count.value++
}
</script>
```

**优势：**
- 复用 Vue 的响应式系统、DevTools
- 接入 Element Plus、Pinia 等生态
- 设计师可直接修改生成的 Vue 代码

### 2.3 方案 B：编译到 React

```御程
// counter.ycv

视图 计数器 {
    状态 count: 整数 = 0
    
    渲染 {
        按钮 {
            文字: "点击 " + 转文本(count) + " 次"
            点击 => { count += 1 }
        }
    }
}
```

↓ 编译为 ↓

```jsx
import { useState } from 'react'

export function 计数器() {
  const [count, setCount] = useState(0)
  
  return (
    <button onClick={() => setCount(count + 1)}>
      点击 {count} 次
    </button>
  )
}
```

### 2.4 工具链支持

```bash
# 编译为 Vue 项目
yucheng build --target vue src/

# 编译为 React 项目
yucheng build --target react src/

# 开发模式（热重载）
yucheng dev --target vue --port 3000

# 生成完整前端项目脚手架
yucheng init --template web-vue my-app
```

### 2.5 前端项目结构

```
my-web-app/
├── src/
│   ├── views/           # .ycv 视图文件
│   │   ├── Home.ycv
│   │   └── Login.ycv
│   ├── logic/           # .yc/.yci 业务逻辑
│   │   ├── auth.yc
│   │   └── auth.yci
│   └── app.ycl          # 链接配置
├── dist/                # 编译输出
│   └── vue/             # 生成的 Vue 项目
│       ├── src/
│       ├── package.json
│       └── vite.config.js
└── yucheng.config.json
```

### 2.6 配置文件

```json
// yucheng.config.json
{
  "web": {
    "target": "vue",
    "version": "3.4",
    "cssFramework": "tailwind",
    "uiLibrary": "element-plus",
    "router": true,
    "stateManagement": "pinia"
  }
}
```

---

## 3. 数据科学探索

### 3.1 策略：提供 Jupyter 内核

**不要做：**
- ❌ 从零实现 Pandas / NumPy
- ❌ 自研绘图库

**正确做法：让 YuCheng 成为 Jupyter 的一个内核**

### 3.2 Jupyter 内核实现

```
┌─────────────────────────────────────────────────────────┐
│                    Jupyter Notebook                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   In [1]: 分析数据("sales.csv")                         │
│   Out[1]: {"A": 120.5, "B": 98.2}                       │
│                                                         │
│   In [2]: %plot bar result                              │
│   Out[2]: [柱状图显示]                                   │
│                                                         │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              YuCheng Jupyter Kernel                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │ ZeroMQ 通信  │  │ REPL 解释器 │  │ Python 桥接 │     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
└─────────────────────────────────────────────────────────┘
```

### 3.3 Python 桥接

通过 C ABI 调用 Python 科学栈：

```御程
// data_analysis.yc

// 导入 Python 模块
外部 python "pandas" as pd
外部 python "matplotlib.pyplot" as plt

// 纯函数：数据处理（高性能）
纯函数 func 计算均值(数据: 列表<小数>) -> 小数
    要求 数据.长度 > 0
{
    var 总和: 小数 = 0.0
    循环 值 在 数据 {
        总和 += 值
    }
    返回 总和 / 转小数(数据.长度)
}

// 副作用函数：调用 Python
副作用 func 分析数据(路径: 文本) -> 字典 {
    var df = pd.read_csv(路径)
    var result = df.groupby("category").mean()
    返回 result.to_dict()
}

// 副作用函数：绘图
副作用 func 绘制柱状图(数据: 字典, 标题: 文本) {
    plt.bar(数据.keys(), 数据.values())
    plt.title(标题)
    plt.show()
}
```

### 3.4 Jupyter 使用示例

```jupyter
In [1]: // 加载数据
        var 销售数据 = 分析数据("sales.csv")
        销售数据

Out[1]: {"电子产品": 15000.5, "服装": 8200.3, "食品": 12000.8}

In [2]: // 使用 YuCheng 的纯函数计算
        var 均值 = 计算均值([15000.5, 8200.3, 12000.8])
        均值

Out[2]: 11733.87

In [3]: // 调用 Python 绘图
        %plot bar 销售数据 "销售额分布"

Out[3]: [显示柱状图]

In [4]: // 内存透视
        %memory

Out[4]: {
          "heap_used": 1024,
          "allocations": 3,
          "leaks": 0
        }
```

### 3.5 魔法命令

| 命令 | 说明 |
|------|------|
| `%plot <type> <data>` | 调用 Matplotlib 绘图 |
| `%memory` | 显示内存状态 |
| `%timeit <expr>` | 性能计时 |
| `%audit` | 运行代码审计 |
| `%export python` | 导出为 Python 代码 |

### 3.6 安装与配置

```bash
# 安装 YuCheng Jupyter 内核
yucheng jupyter install

# 验证安装
jupyter kernelspec list
# 输出: yucheng    /usr/local/share/jupyter/kernels/yucheng

# 启动 Jupyter
jupyter notebook
# 新建笔记本时选择 "YuCheng" 内核
```

---

## 4. 大型 GUI 桌面应用

### 4.1 策略：深度集成 Tauri

**不要做：**
- ❌ 自研 UI 渲染引擎
- ❌ 重复实现 Flutter 的 Widget 树

**正确做法：将 `.ycv` 升级为声明式 UI DSL，由 Tauri 渲染**

### 4.2 架构设计

```
┌─────────────────────────────────────────────────────────┐
│                    Tauri 应用                            │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   ┌─────────────────────┐  ┌─────────────────────┐     │
│   │    前端 (WebView)    │  │   后端 (YuCheng)    │     │
│   │                     │  │                     │     │
│   │  .ycv → HTML/CSS/JS │◄─┤  .yc/.yci 业务逻辑  │     │
│   │  响应式 UI 渲染      │  │  内存安全执行       │     │
│   │                     │  │  契约验证           │     │
│   └─────────────────────┘  └─────────────────────┘     │
│              │                       │                  │
│              └───── invoke() ────────┘                  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 4.3 响应式 .ycv 语法

```御程
// main.ycv

视图 主窗口 {
    // 响应式状态
    状态 用户名: 文本 = ""
    状态 是否有效: 布尔 = 假
    状态 提交中: 布尔 = 假
    
    // 计算属性（自动响应状态变化）
    计算 按钮文字: 文本 {
        如果 提交中 {
            返回 "提交中..."
        }
        返回 "提交"
    }
    
    // 副作用：监听状态变化
    监听 用户名 {
        是否有效 = 用户名.长度 > 3
    }
    
    // 方法
    副作用 func 提交() {
        提交中 = 真
        // 调用后端逻辑
        var 结果 = 后端.验证用户(用户名)
        提交中 = 假
        
        如果 结果.成功 {
            导航到(主页)
        }
    }
    
    // 渲染
    渲染 {
        垂直布局 {
            间距: 16
            内边距: 24
            
            输入框 {
                绑定: 用户名
                占位符: "请输入用户名"
                样式: {
                    宽度: "100%"
                    边框: 是否有效 ? "2px solid green" : "1px solid gray"
                }
            }
            
            按钮 {
                文字: 按钮文字
                禁用: !是否有效 || 提交中
                点击 => 提交()
                样式: {
                    背景色: "#1976D2"
                    颜色: "white"
                }
            }
        }
    }
}
```

### 4.4 后端逻辑 (.yc/.yci)

```御程
// backend.yc

纯函数 func 验证用户名格式(用户名: 文本) -> 布尔
    要求 用户名.长度 > 0

副作用 func 验证用户(用户名: 文本) -> 验证结果

// backend.yci

纯函数 func 验证用户名格式(用户名: 文本) -> 布尔 {
    如果 用户名.长度 < 3 {
        返回 假
    }
    // 只允许字母数字
    循环 字符 在 用户名 {
        如果 !是字母数字(字符) {
            返回 假
        }
    }
    返回 真
}

副作用 func 验证用户(用户名: 文本) -> 验证结果 {
    如果 !验证用户名格式(用户名) {
        返回 { 成功: 假, 错误: "用户名格式不正确" }
    }
    
    // 调用 API
    var 响应 = HTTP_POST("/api/validate", { "username": 用户名 })
    返回 响应
}
```

### 4.5 Tauri 命令绑定

编译器自动生成 Tauri 命令：

```rust
// 自动生成的 src-tauri/src/commands.rs

#[tauri::command]
fn 验证用户(用户名: String) -> Result<验证结果, String> {
    // 调用 YuCheng 编译的库
    yucheng_runtime::call("验证用户", &[用户名])
}
```

### 4.6 工具链

```bash
# 创建 Tauri 项目
yucheng init --template desktop my-app

# 开发模式（热重载）
yucheng dev --target tauri

# 构建发布版
yucheng build --target tauri --release

# 输出
# Windows: my-app.exe
# macOS: my-app.app
# Linux: my-app.AppImage
```

### 4.7 项目结构

```
my-desktop-app/
├── src/
│   ├── views/           # .ycv 视图文件
│   │   ├── Main.ycv
│   │   └── Settings.ycv
│   ├── logic/           # .yc/.yci 后端逻辑
│   │   ├── core.yc
│   │   └── core.yci
│   └── app.ycl
├── src-tauri/           # Tauri 配置（自动生成）
│   ├── src/
│   │   ├── main.rs
│   │   └── commands.rs  # 自动生成的命令绑定
│   ├── Cargo.toml
│   └── tauri.conf.json
└── yucheng.config.json
```

---

## 5. 编译目标总览

| 目标 | 命令 | 输出 | 适用场景 |
|------|------|------|---------|
| 原生可执行 | `--target native` | `.exe` / 二进制 | CLI 工具、系统服务 |
| 动态库 | `--target lib` | `.dll` / `.so` | 跨语言调用 |
| Vue | `--target vue` | Vue 项目 | Web 前端 |
| React | `--target react` | React 项目 | Web 前端 |
| Tauri | `--target tauri` | 桌面应用 | GUI 应用 |
| Jupyter | `--target jupyter` | Kernel | 数据科学 |
| WASM | `--target wasm` | `.wasm` | 浏览器/边缘计算 |

---

## 6. 跨平台配置

```json
// yucheng.config.json
{
  "project": {
    "name": "我的应用",
    "version": "1.0.0"
  },
  
  "targets": {
    "web": {
      "framework": "vue",
      "output": "dist/web"
    },
    "desktop": {
      "framework": "tauri",
      "output": "dist/desktop",
      "platforms": ["windows", "macos", "linux"]
    },
    "jupyter": {
      "pythonBridge": true,
      "magicCommands": ["plot", "memory", "audit"]
    }
  },
  
  "bridges": {
    "python": {
      "enabled": true,
      "modules": ["pandas", "numpy", "matplotlib"]
    }
  }
}
```

---

## 7. 最佳实践

### 7.1 分层架构

```
┌─────────────────────────────────────────────────────────┐
│                    表现层 (.ycv)                         │
│              Web / Desktop / Mobile                      │
├─────────────────────────────────────────────────────────┤
│                    业务逻辑层 (.yc/.yci)                  │
│           纯函数 + 查询 + 副作用（平台无关）              │
├─────────────────────────────────────────────────────────┤
│                    桥接层                                │
│         Vue / React / Tauri / Python / C ABI            │
└─────────────────────────────────────────────────────────┘
```

### 7.2 代码复用

```御程
// shared/validation.yc - 跨平台共享

纯函数 func 验证邮箱(邮箱: 文本) -> 布尔
纯函数 func 验证手机号(手机: 文本) -> 布尔
纯函数 func 计算密码强度(密码: 文本) -> 整数

// 这些纯函数可以在 Web、Desktop、Jupyter 中复用
// 编译器会根据目标平台生成对应代码
```

### 7.3 平台特定代码

```御程
// platform.yc

#如果 目标 == "web"
副作用 func 存储数据(键: 文本, 值: 文本) {
    localStorage.setItem(键, 值)
}
#否则如果 目标 == "desktop"
副作用 func 存储数据(键: 文本, 值: 文本) {
    写入文件("data/" + 键 + ".json", 值)
}
#结束
```

---

## 8. 路线图

| 阶段 | 目标 | 时间 |
|------|------|------|
| Phase 1 | 原生编译 + C ABI | 已完成 |
| Phase 2 | Tauri 桌面应用 | Q2 2026 |
| Phase 3 | Vue/React Web 编译 | Q3 2026 |
| Phase 4 | Jupyter 内核 | Q4 2026 |
| Phase 5 | WASM 编译 | Q1 2027 |
