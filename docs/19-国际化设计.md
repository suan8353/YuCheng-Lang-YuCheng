# YuCheng 语言 - 国际化设计

> **实现状态**: ⚠️ 基础实现
> - ✅ 中文关键字完整支持（100+关键字）
> - ✅ 英文关键字完整支持（完整映射表）
> - ✅ 混合模式完整支持（中英文可混用）
> - ✅ 多语言框架（`ui/i18n.rs` - 10种语言）
> - ✅ 词法分析器支持（LanguageMode: Chinese/English/Mixed）
> - ✅ 关键字映射表（`keywords.rs`）
> - ✅ 测试验证（英文关键字测试、混合模式测试）
> - ❌ 代码转换工具 (yucheng translate) - 未实现
> - ❌ IDE双语支持 - 未实现
> 
> **完成报告**: `.project-reports/implementation-status/19-I18N-COMPLETION.md`  
> **示例代码**: `examples/32-english-working.ycs`, `examples/31-mixed-keywords.ycs`

## 1. 设计理念

YuCheng 采用**关键字映射**方案实现国际化，而非维护两套独立语言。

### 1.1 核心原则

```
┌─────────────────────────────────────────────────────────────┐
│                      YuCheng 编译器                          │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │  中文源码    │ → │  统一 AST   │ → │  目标代码    │     │
│  │  English    │ → │  Unified    │ → │  Target     │     │
│  │  混合模式    │ → │             │ → │             │     │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

**一套语法规则 + 一个编译器 + 一个生态系统**

### 1.2 为什么不做两套语言？

| 方案 | 维护成本 | 生态兼容 | 团队协作 |
|------|---------|---------|---------|
| 两套独立语言 | 翻倍 | 库不兼容 | 困难 |
| 关键字映射 | 单一 | 完全兼容 | 无缝 |

---

## 2. 关键字映射表

### 2.1 函数分类

| 中文 | English | 说明 |
|------|---------|------|
| `纯函数` | `pure` | 无副作用的计算函数 |
| `查询` | `query` | 只读外部状态 |
| `副作用` | `effect` | 可修改状态 |
| `异步` | `async` | 异步函数 |
| `func` | `func` | 函数声明（通用） |

### 2.2 控制流

| 中文 | English | 说明 |
|------|---------|------|
| `如果` | `if` | 条件判断 |
| `否则` | `else` | 否则分支 |
| `否则如果` | `elif` | 否则如果 |
| `匹配` | `match` | 模式匹配 |
| `当` | `while` | 循环 |
| `循环` | `for` | 遍历循环 |
| `在` | `in` | 遍历介词 |
| `中断` | `break` | 跳出循环 |
| `继续` | `continue` | 继续下一轮 |
| `返回` | `return` | 返回值 |

### 2.3 类型系统

| 中文 | English | 说明 |
|------|---------|------|
| `类型` | `type` | 类型定义 |
| `整数` | `int` | 整数类型 |
| `小数` | `float` | 浮点类型 |
| `文本` | `string` | 字符串类型 |
| `布尔` | `bool` | 布尔类型 |
| `真` | `true` | 真值 |
| `假` | `false` | 假值 |
| `空` | `null` | 空值 |
| `列表` | `list` | 列表类型 |
| `映射` | `map` | 映射类型 |
| `可选` | `optional` | 可选类型 |
| `结果` | `result` | 结果类型 |

### 2.4 契约系统

| 中文 | English | 说明 |
|------|---------|------|
| `要求` | `requires` | 前置条件 |
| `保证` | `ensures` | 后置条件 |
| `断言` | `assert` | 断言 |
| `返回值` | `result` | 后置条件中的返回值 |

### 2.5 内存管理

| 中文 | English | 说明 |
|------|---------|------|
| `申请` | `alloc` | 分配内存 |
| `释放` | `free` | 释放内存 |
| `持有` | `retain` | 延长生命周期 |
| `var` | `var` | 变量声明（通用） |
| `const` | `const` | 常量声明（通用） |

### 2.6 错误处理

| 中文 | English | 说明 |
|------|---------|------|
| `尝试` | `try` | 尝试块 |
| `捕获` | `catch` | 捕获异常 |
| `最终` | `finally` | 最终块 |
| `成功` | `ok` | 成功结果 |
| `失败` | `err` | 失败结果 |

### 2.7 并发

| 中文 | English | 说明 |
|------|---------|------|
| `等待` | `await` | 等待异步 |
| `通道` | `channel` | 通道 |
| `并行` | `parallel` | 并行块 |
| `任务` | `task` | 任务 |
| `锁定` | `lock` | 加锁 |

### 2.8 模块系统

| 中文 | English | 说明 |
|------|---------|------|
| `导入` | `import` | 导入模块 |
| `导出` | `export` | 导出符号 |
| `模块` | `module` | 模块声明 |

### 2.9 界面系统

| 中文 | English | 说明 |
|------|---------|------|
| `窗口` | `window` | 窗口 |
| `视图` | `view` | 视图 |
| `组件` | `component` | 组件 |
| `状态` | `state` | 状态 |
| `绑定` | `bind` | 数据绑定 |

### 2.10 测试

| 中文 | English | 说明 |
|------|---------|------|
| `测试` | `test` | 测试块 |
| `断言_相等` | `assert_eq` | 相等断言 |
| `断言_不等` | `assert_ne` | 不等断言 |

---

## 3. 语法对照示例

### 3.1 中文版

```御程
// calculator.yc - 中文版

类型 计算结果 {
    值: 小数
    有效: 布尔
}

纯函数 func 加法(a: 小数, b: 小数) -> 小数
    保证 返回值 == a + b

纯函数 func 除法(a: 小数, b: 小数) -> 结果<小数, 文本>
    要求 b <> 0.0

副作用 func 主函数() {
    var x = 10.0
    var y = 3.0
    
    如果 y <> 0.0 {
        匹配 除法(x, y) {
            成功(结果) => 控制台 => "结果: ", 结果
            失败(错误) => 控制台 => "错误: ", 错误
        }
    } 否则 {
        控制台 => "除数不能为零"
    }
}
```

### 3.2 English Version

```yucheng
// calculator.yc - English version

type CalcResult {
    value: float
    valid: bool
}

pure func add(a: float, b: float) -> float
    ensures result == a + b

pure func divide(a: float, b: float) -> result<float, string>
    requires b <> 0.0

effect func main() {
    var x = 10.0
    var y = 3.0
    
    if y <> 0.0 {
        match divide(x, y) {
            ok(res) => console => "Result: ", res
            err(e) => console => "Error: ", e
        }
    } else {
        console => "Cannot divide by zero"
    }
}
```

### 3.3 混合模式

```yucheng
// 混合模式 - 关键字用英文，标识符用中文
// Mixed mode - English keywords, Chinese identifiers

type 计算结果 {
    值: float
    有效: bool
}

pure func 加法(a: float, b: float) -> float
    ensures result == a + b

effect func main() {
    var 数值 = 10.0
    
    if 数值 > 0.0 {
        console => "正数: ", 数值
    }
}
```

---

## 4. 配置方式

### 4.1 项目级配置

```toml
# yucheng.toml

[语言]
# 关键字模式: "中文" | "english" | "mixed"
关键字模式 = "中文"

# 混合模式下的偏好
[语言.混合]
函数分类 = "中文"      # 纯函数/查询/副作用
控制流 = "english"     # if/else/match
类型 = "中文"          # 整数/小数/文本
```

### 4.2 文件级覆盖

```御程
// 在文件开头声明
#语言 english

pure func add(a: int, b: int) -> int {
    return a + b
}
```

### 4.3 IDE 设置

```json
// .vscode/settings.json
{
  "yucheng.language.keywords": "中文",
  "yucheng.language.displayMode": "dual",  // 同时显示中英文
  "yucheng.language.autoComplete": "both"  // 补全时显示两种
}
```

---

## 5. 编译器实现

### 5.1 词法分析器

```rust
// lexer.rs

pub struct KeywordMap {
    chinese: HashMap<&'static str, TokenKind>,
    english: HashMap<&'static str, TokenKind>,
}

impl KeywordMap {
    pub fn new() -> Self {
        let mut chinese = HashMap::new();
        let mut english = HashMap::new();
        
        // 函数分类
        chinese.insert("纯函数", TokenKind::Pure);
        english.insert("pure", TokenKind::Pure);
        
        chinese.insert("查询", TokenKind::Query);
        english.insert("query", TokenKind::Query);
        
        chinese.insert("副作用", TokenKind::Effect);
        english.insert("effect", TokenKind::Effect);
        
        // 控制流
        chinese.insert("如果", TokenKind::If);
        english.insert("if", TokenKind::If);
        
        chinese.insert("否则", TokenKind::Else);
        english.insert("else", TokenKind::Else);
        
        // ... 更多映射
        
        Self { chinese, english }
    }
    
    pub fn lookup(&self, word: &str, mode: LanguageMode) -> Option<TokenKind> {
        match mode {
            LanguageMode::Chinese => self.chinese.get(word).copied(),
            LanguageMode::English => self.english.get(word).copied(),
            LanguageMode::Mixed => {
                self.chinese.get(word)
                    .or_else(|| self.english.get(word))
                    .copied()
            }
        }
    }
}
```

### 5.2 AST 统一

```rust
// ast.rs - 与语言无关的 AST

pub enum FunctionKind {
    Pure,    // 纯函数 / pure
    Query,   // 查询 / query
    Effect,  // 副作用 / effect
}

pub struct FunctionDecl {
    pub kind: FunctionKind,
    pub name: String,
    pub params: Vec<Parameter>,
    pub return_type: Option<Type>,
    pub contracts: Vec<Contract>,
    pub body: Option<Block>,
}

// AST 不关心源码是中文还是英文
// 编译后的结果完全相同
```

---

## 6. 代码转换工具

### 6.1 中英互转

```bash
# 中文转英文
yucheng translate --to english src/

# 英文转中文
yucheng translate --to chinese src/

# 预览转换结果
yucheng translate --to english --dry-run src/main.yc
```

### 6.2 转换示例

```
$ yucheng translate --to english src/main.yc

转换前 (中文):
  纯函数 func 加法(a: 整数, b: 整数) -> 整数 {
      返回 a + b
  }

转换后 (English):
  pure func add(a: int, b: int) -> int {
      return a + b
  }

[应用转换] [取消]
```

### 6.3 标识符保留

```bash
# 仅转换关键字，保留标识符
yucheng translate --to english --keep-identifiers src/

# 结果：
pure func 加法(a: int, b: int) -> int {
    return a + b
}
```

---

## 7. IDE 支持

### 7.1 双语显示

```
┌─────────────────────────────────────────────────────────────┐
│ 纯函数 func 加法(a: 整数, b: 整数) -> 整数                    │
│ ~~~~~~      ~~~~    ~~~~    ~~~~      ~~~~                  │
│ pure        add     int     int       int     ← 悬浮提示    │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 智能补全

```
输入 "pu" 时显示：
┌─────────────────────────┐
│ 💡 pure    (纯函数)      │
│ 💡 纯函数  (pure)        │
└─────────────────────────┘

输入 "纯" 时显示：
┌─────────────────────────┐
│ 💡 纯函数  (pure)        │
└─────────────────────────┘
```

### 7.3 文档双语

```
悬浮在 "纯函数" 上：
┌─────────────────────────────────────────────────────────────┐
│ 纯函数 (pure function)                                       │
│ ─────────────────────────────────────────────────────────── │
│ 声明一个无副作用的计算函数。                                  │
│ Declares a side-effect-free computation function.           │
│                                                             │
│ 约束 / Constraints:                                         │
│ • 不能调用副作用函数 / Cannot call effect functions          │
│ • 相同输入返回相同输出 / Same input yields same output       │
└─────────────────────────────────────────────────────────────┘
```

---

## 8. 跨语言协作

### 8.1 团队混合开发

```
项目结构：
├── src/
│   ├── core/           # 中文团队维护
│   │   ├── 计算.yc
│   │   └── 计算.yci
│   ├── api/            # 英文团队维护
│   │   ├── handler.yc
│   │   └── handler.yci
│   └── shared/         # 共享接口（双语注释）
│       └── types.yc
```

### 8.2 库发布

```toml
# 发布时自动生成双语版本
[发布]
生成英文版 = true
生成中文版 = true

# 包含两种语言的类型定义
[发布.类型定义]
格式 = ["中文", "english"]
```

### 8.3 导入兼容

```御程
// 中文项目导入英文库
导入 "english-lib" as 英文库

// 可以用中文调用
var 结果 = 英文库.calculate(10, 20)

// 也可以用英文调用
var result = english_lib.calculate(10, 20)
```

---

## 9. 文档国际化

### 9.1 文档结构

```
docs/
├── zh/                 # 中文文档
│   ├── 01-架构文档.md
│   ├── 02-开发文档.md
│   └── ...
├── en/                 # English docs
│   ├── 01-architecture.md
│   ├── 02-development.md
│   └── ...
└── shared/             # 共享资源（图表等）
    └── diagrams/
```

### 9.2 文档同步

```bash
# 检查文档同步状态
yucheng docs --check-sync

# 输出：
中文文档: 18 篇
英文文档: 15 篇
缺失翻译: 3 篇
  - 16-并发与异步.md
  - 17-包管理系统.md
  - 18-气泡教程系统.md
```

---

## 10. 最佳实践

### 10.1 开源项目

```toml
# 推荐：使用英文关键字 + 英文标识符
# 便于国际社区贡献

[语言]
关键字模式 = "english"
```

### 10.2 企业内部项目

```toml
# 推荐：使用中文关键字 + 中文标识符
# 降低团队学习成本

[语言]
关键字模式 = "中文"
```

### 10.3 教学场景

```toml
# 推荐：混合模式
# 中文关键字帮助理解，英文类型名对接行业

[语言]
关键字模式 = "mixed"

[语言.混合]
函数分类 = "中文"
控制流 = "中文"
类型 = "english"  # int, float, string
```

---

## 11. 路线图

| 阶段 | 内容 | 状态 |
|------|------|------|
| Phase 1 | 中文关键字完整支持 | ✅ 完成 |
| Phase 2 | 英文关键字映射 | 🔄 进行中 |
| Phase 3 | 混合模式支持 | 📅 计划中 |
| Phase 4 | 代码转换工具 | 📅 计划中 |
| Phase 5 | IDE 双语支持 | 📅 计划中 |
| Phase 6 | 文档国际化 | 📅 计划中 |
