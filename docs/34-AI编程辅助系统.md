# AI 编程辅助系统

**状态**: ⚠️ 基础框架实现，核心AI功能未真正实现

**最后更新**: 2026-02-10

**实现报告**: 
- 验证报告: `.project-reports/implementation-status/DOC_34_AI_ASSIST_VERIFICATION_REPORT.md`
- 后端实现报告: `.project-reports/implementation-status/DOC_34_AI_ASSIST_IMPLEMENTATION_REPORT.md`
- 前端实现报告: `.project-reports/implementation-status/DOC_34_AI_ASSIST_FRONTEND_IMPLEMENTATION_REPORT.md`

**已实现**:
- ✅ AI客户端框架 (ai_client.rs)
- ✅ AI管道框架 (ai_pipeline.rs) - 包含TODO
- ✅ AI验证器 (ai_validator.rs)
- ✅ AI标准库函数 (8个)
- ⚠️ Tauri命令 (15个) - 大部分返回硬编码/假数据，未真正调用AI模型
- ⚠️ 聊天面板 - 仅有UI框架，Chat Panel实际UI不存在
- ⚠️ 辅助面板 - 仅有UI框架
- ✅ 右键菜单集成 (7个AI功能) - UI已实现
- ❌ 内联补全功能 - 返回固定示例，非AI驱动
- ❌ AI代码修复 - 返回模板代码，非真实AI修复
- ❌ AI代码解释 - 返回固定格式，非真实分析
- ❌ AI重构建议 - 返回固定建议，非真实分析
- ❌ AI测试生成 - 返回示例模板，非真实生成
- ❌ AI文档生成 - 返回模板，非真实生成

## 概述

YuCheng AI 编程助手是一个智能代码生成系统，核心理念是：**"AI 只填空，你控结构；编译器是最终裁判"**。

## 支持的 AI 模型

### 国际模型
| 提供商 | 模型 | 特点 |
|--------|------|------|
| OpenAI | GPT-4o, GPT-4o-mini, o1 | 综合能力强 |
| Claude | Claude 3.5 Sonnet, Haiku | 代码理解优秀 |
| Google Gemini | Gemini 2.0, 1.5 Pro/Flash | 多模态支持 |

### 国内模型
| 提供商 | 模型 | 特点 |
|--------|------|------|
| DeepSeek | deepseek-chat, deepseek-coder | 代码专精，性价比高 |
| 通义千问 | qwen-turbo, qwen-max | 阿里云，中文优化 |
| 智谱 | GLM-4, GLM-4-Flash | 清华系，理解力强 |
| Kimi | moonshot-v1-8k/32k/128k | 长上下文支持 |

### 本地模型
| 提供商 | 模型 | 特点 |
|--------|------|------|
| Ollama | Llama3, CodeLlama, Qwen2 | 本地运行，隐私安全 |

### MCP 协议
支持 Model Context Protocol，可连接任意兼容的 AI 服务。

## 工作流程

```
用户需求 → IDE 规则转译 → 大模型生成 → 编译验证 → 错误修复 → 写入文件
```

### 四阶段生成
1. **需求解析**: 自然语言 → JSON 规格
2. **骨架生成**: 生成函数签名和契约
3. **AI 填充**: 大模型填充函数体
4. **编译验证**: 语法检查 + 自动修复循环

## 配置方式

### 环境变量
```bash
export YUCHENG_AI_PROVIDER=deepseek
export YUCHENG_AI_KEY=your-api-key
export YUCHENG_AI_MODEL=deepseek-chat
export YUCHENG_AI_STREAM=true
```

### 配置文件 (ai-config.toml)
```toml
provider = "gemini"
api_key = "your-api-key"
model = "gemini-1.5-flash"
max_tokens = 4096
temperature = 0.7
stream = true

# MCP 服务器配置 (可选)
[mcp_server]
command = "npx"
args = ["-y", "@anthropic/mcp-server"]
```

## CLI 命令

```bash
# 从自然语言生成代码
yucheng ai "创建一个计算器程序"

# 指定输出目录
yucheng ai "用户登录模块" --output ./src

# 启用 AI 填充函数体
yucheng ai "排序算法" --fill

# 从 JSON 规格生成
yucheng scaffold spec.json --output ./src
```

## IDE 功能

### 右键菜单 (Ctrl+右键)
- ✨ **生成实现** (Ctrl+Shift+G) - 为函数签名生成实现
- 📝 **补全代码** (Ctrl+Space) - 智能代码补全
- 💡 **解释代码** (Ctrl+Shift+E) - 解释选中代码
- 🔧 **重构建议** (Ctrl+Shift+R) - 代码优化建议
- 🔨 **修复错误** (Ctrl+.) - 自动修复编译错误
- 🧪 **生成测试** (Ctrl+Shift+T) - 生成单元测试
- 📖 **生成文档** (Ctrl+Shift+D) - 生成注释文档

### 内联补全
- 输入时自动触发
- 可配置延迟 (100-1000ms)
- Tab 接受，Esc 取消

### 流式输出
- 实时显示生成内容
- 可随时停止生成
- 支持复制/插入/应用

## 多语言支持

AI 助手支持中英文双语交互：

```typescript
// 设置提示词语言
settings.promptLanguage = 'zh-CN'  // 或 'en'
```

系统会根据语言设置调整：
- 系统提示词
- 代码生成提示
- 错误修复提示
- 用户界面文本

## API 使用

### Rust API
```rust
use yucheng_compiler::{AiClient, AiClientConfig, AiProvider, ChatMessage};

// 创建客户端
let config = AiClientConfig {
    provider: AiProvider::Gemini,
    api_key: "your-key".to_string(),
    model: "gemini-1.5-flash".to_string(),
    stream: true,
    ..Default::default()
};
let client = AiClient::new(config);

// 普通请求
let response = client.ask("写一个阶乘函数")?;

// 流式请求
client.chat_stream(messages, Box::new(|chunk| {
    print!("{}", chunk);
}))?;

// 内联补全
let completion = client.inline_complete(prefix, suffix, "zh-CN")?;
```

### 多语言提示词
```rust
use yucheng_compiler::MultiLangPrompts;

let prompts = MultiLangPrompts::new("zh-CN");
let system = prompts.system_prompt();
let gen_prompt = prompts.code_gen_prompt("计算器");
```

## AI 设置面板

IDE 提供专用的 AI 设置面板：

1. **提供商选择** - 可视化选择 AI 服务商
2. **API 配置** - 密钥、模型、自定义 URL
3. **生成配置** - Token 数、温度、流式输出
4. **功能开关** - 内联补全、自动验证、右键菜单
5. **语言设置** - 提示词语言选择
6. **MCP 配置** - 服务器命令和参数
7. **连接测试** - 验证 API 配置是否正确

## AI 代码验证器

AI 生成的代码会经过多层验证，确保质量：

### 验证类型

| 验证类型 | 说明 | 自动修复 |
|----------|------|----------|
| 语法验证 | 检查代码语法正确性 | ✅ |
| 规格一致性 | .yc 与 .yci 函数签名匹配 | ❌ |
| 契约验证 | 检查前置/后置条件 | 建议 |
| 函数分类 | 纯函数/查询/副作用一致性 | 建议 |
| 阻塞检查 | UI函数中的阻塞调用 | 建议 |
| 可访问性 | UI组件无障碍标签 | ✅ |
| 安全检查 | 除零、越界等风险 | 建议 |

### 验证配置

```rust
use yucheng_compiler::{AiValidator, AiValidatorConfig};

let config = AiValidatorConfig {
    syntax_check: true,
    spec_consistency: true,
    contract_check: true,
    category_check: true,
    blocking_check: true,
    accessibility_check: true,
    safety_check: true,
    max_errors: 50,
    language: "zh-CN".to_string(),
};

let mut validator = AiValidator::new(config);
let report = validator.validate(code, "example.ycs");

if !report.valid {
    for error in &report.syntax_errors {
        println!("错误: {}", error.message);
    }
    for suggestion in &report.fix_suggestions {
        println!("建议: {}", suggestion.description);
    }
}
```

### 规格一致性检查

```rust
// 检查 .yc 规格与 .yci 实现是否一致
let issues = validator.validate_spec_consistency(
    spec_code,   // .yc 文件内容
    impl_code,   // .yci 文件内容
    "calc.yc",
    "calc.yci"
);

for issue in issues {
    match issue.issue_type {
        SpecIssueType::MissingImplementation => 
            println!("缺少实现: {}", issue.function_name),
        SpecIssueType::SignatureMismatch => 
            println!("签名不匹配: {}", issue.function_name),
        _ => {}
    }
}
```

## IDE 独有智能功能

### 智能签名生成

根据函数名和描述自动推断签名：

```rust
use yucheng_compiler::IdeAiFeatures;

let features = IdeAiFeatures::new("zh-CN");

// 输入: 函数名 + 描述
let sig = features.generate_signature("计算和", "计算两个数的和");
// 输出: 纯函数 func 计算和(a: 整数, b: 整数) -> 整数
```

### 智能契约建议

根据函数签名推荐契约：

```rust
let contracts = features.suggest_contracts(
    "纯函数 func 除法(a: 整数, 除数: 整数) -> 整数"
);
// 输出: ["要求 除数 != 0", "保证 result >= 0"]
```

### 智能测试生成

为函数自动生成测试用例：

```rust
let test_code = features.generate_tests("加法", signature);
// 生成包含基本测试和边界测试的测试套件
```

### 智能文档生成

自动生成函数文档注释：

```rust
let doc = features.generate_doc("加法", signature, body);
// 生成包含分类、参数、返回值、示例的文档
```

### 重构建议

分析代码并给出重构建议：

```rust
let suggestions = features.suggest_refactoring(code);
for s in suggestions {
    println!("[{}] {}", s.priority, s.description);
}
// 输出: [高] 函数过长，建议拆分为多个小函数
// 输出: [中] 发现魔法数字，建议提取为常量
```

## 安全说明

- API Key 本地存储，不上传服务器
- 支持本地 Ollama 模型，完全离线
- 代码仅发送给选定的 AI 服务商
- MCP 协议支持自建服务器

## 获取 API Key

| 服务商 | 获取地址 |
|--------|----------|
| OpenAI | https://platform.openai.com/api-keys |
| Claude | https://console.anthropic.com/ |
| Gemini | https://aistudio.google.com/apikey |
| DeepSeek | https://platform.deepseek.com/ |
| 通义千问 | https://dashscope.console.aliyun.com/ |
| 智谱 | https://open.bigmodel.cn/ |
| Kimi | https://platform.moonshot.cn/ |
| Ollama | 本地运行，无需 Key |


## IDE 后端 API (Tauri 命令)

IDE 后端提供以下 Tauri 命令供前端调用：

### 新版 API (推荐)

| 命令 | 说明 | 参数 |
|------|------|------|
| `ai_generate_with_validation` | 带验证的完整生成流程 | requirement, projectName, multiFile, hasUi, aiFill, configJson |
| `ai_complete_code` | AI 代码补全 | prefix, suffix, configJson |
| `ai_fix_error` | AI 修复编译错误 | code, errorMessage, configJson |
| `ai_explain_code` | AI 解释代码 | code, configJson |
| `ai_suggest_refactor` | AI 重构建议 | code, configJson |
| `ai_generate_tests` | AI 生成测试 | funcSignature, configJson |
| `ai_generate_doc` | AI 生成文档 | funcCode, configJson |
| `ai_validate_code` | AI 代码验证 | code, filename, configJson |
| `ai_get_model_info` | 获取模型信息 | configJson |
| `ai_is_configured` | 检查 AI 是否配置 | configJson |
| `ai_get_providers` | 获取支持的提供商列表 | - |

### 配置格式

```typescript
interface AiConfigInput {
  provider: string;      // openai, claude, gemini, deepseek, qwen, zhipu, kimi, ollama
  api_key: string;
  model?: string;
  base_url?: string;
  language?: string;     // zh-CN, en
  auto_fix?: boolean;
  max_retries?: number;
}
```

### 返回格式

```typescript
// ai_generate_with_validation 返回
interface AiGenerationResultV2 {
  success: boolean;
  files: Array<{ filename: string; content: string; file_type: string }>;
  validation: {
    valid: boolean;
    syntax_errors: number;
    contract_issues: number;
    category_issues: number;
    safety_issues: number;
    fix_suggestions: string[];
  } | null;
  errors: string[];
  warnings: string[];
  retries: number;
  model_used: string;
}

// ai_complete_code, ai_fix_error 等返回
interface AiCompletionResult {
  success: boolean;
  completion: string;
  error: string | null;
}

// ai_validate_code 返回
interface AiValidationReport {
  valid: boolean;
  syntax_errors: Array<{ message: string; line: number; column: number; severity: string }>;
  friendly_errors: Array<{ title: string; message: string; category: string; ... }>;
  fix_suggestions: Array<{ description: string; replacement: string | null; line: number | null }>;
}
```

### 使用示例

```typescript
import { invoke } from '@tauri-apps/api/core';

// 带验证的代码生成
const result = await invoke('ai_generate_with_validation', {
  requirement: '创建一个计算器',
  projectName: 'calculator',
  multiFile: true,
  hasUi: true,
  aiFill: true,
  configJson: JSON.stringify({
    provider: 'deepseek',
    api_key: 'your-key',
    model: 'deepseek-chat',
  }),
});

// AI 代码补全
const completion = await invoke('ai_complete_code', {
  prefix: '纯函数 func 加法(a: 整数, b: 整数) -> 整数 {\n    ',
  suffix: '\n}',
  configJson: null,  // 使用环境变量配置
});

// AI 修复错误
const fixed = await invoke('ai_fix_error', {
  code: '纯函数 func 加法(a: 整数, b: 整数 -> 整数 { 返回 a + b }',
  errorMessage: '期望 )，但得到 ->',
  configJson: null,
});
```

## 完整集成模块 (ai_integration.rs)

`AiProgrammingAssistant` 是 AI 编程助手的主入口，整合了所有 AI 功能：

```rust
use yucheng_compiler::{AiProgrammingAssistant, AssistantConfig, GenerateRequest};

// 创建助手
let config = AssistantConfig::default();
let mut assistant = AiProgrammingAssistant::new(config);

// 检查是否配置了 AI
if assistant.has_ai() {
    println!("使用模型: {}", assistant.model_info());
}

// 生成代码
let request = GenerateRequest {
    requirement: "创建一个计算器".to_string(),
    project_name: Some("calc".to_string()),
    multi_file: true,
    has_ui: true,
    ai_fill: true,
};

let result = assistant.generate(&request);

if result.success {
    for file in &result.files {
        println!("生成: {} ({} bytes)", file.filename, file.content.len());
    }
    if let Some(v) = &result.validation {
        println!("验证: {} 语法错误, {} 契约问题", v.syntax_errors, v.contract_issues);
    }
}

// IDE 辅助功能
let completion = assistant.complete_code("纯函数 func ", "")?;
let explanation = assistant.explain_code("纯函数 func 加法...")?;
let fixed = assistant.fix_compile_error(code, error)?;
let tests = assistant.generate_tests("纯函数 func 加法...")?;
let doc = assistant.generate_doc("纯函数 func 加法...")?;
```
